# PostgreSQL enum values are intentionally repeated in DDL. S1192 is useful for
# application code but creates false positives for declarative schema migrations.
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=sql:S1192
sonar.issue.ignore.multicriteria.e1.resourceKey=migrations/**/*.sql
